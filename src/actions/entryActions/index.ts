import { publishEntry } from "./publish-entry.action";
import { fetchSubmission } from "./fetch-submission.action";

const EntryActions = {
  publishEntry,
  fetchSubmission,
};

export { EntryActions };
